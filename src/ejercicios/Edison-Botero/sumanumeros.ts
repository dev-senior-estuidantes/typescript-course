let numeros:number[] = [25,26,27,28,29,30]

for(let i=0; i<numeros.length;i++){
    numeros[0];
    numeros[1];
    numeros[2];
    numeros[3];
    numeros[4];
    numeros[5];
}
var suma1:number=(numeros[0]+numeros[1]+numeros[2]+numeros[3]+numeros[4]+numeros[5]);
console.log(`la suma de los numeros es:     ${suma1}`);